import { ConcatenaNomApePipe } from './concatena-nom-ape.pipe';

describe('ConcatenaNomApePipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatenaNomApePipe();
    expect(pipe).toBeTruthy();
  });
});
